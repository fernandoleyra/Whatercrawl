Interact with a webpage using browser actions (click, fill, scroll) and return the resulting content.

If the user described what they need to do on a page, use that to build the action sequence.
If not, ask: "What URL and what actions do you need to perform on the page?"

Use the watercrawl-interact skill to execute and return the content.
