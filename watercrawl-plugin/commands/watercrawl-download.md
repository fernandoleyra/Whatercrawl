Download an entire website as local markdown files using the local Watercrawl API.

If the user provided a URL, use it. If not, ask: "Which website should I download?"
Ask: "Where should I save the files? (default: ./watercrawl-output)"

Use the watercrawl-download skill to crawl and write the files.
